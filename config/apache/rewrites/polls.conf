# Route requests for a `Poll`.
RewriteRule	^/polls/([a-z0-9\-]+)$				/polls/http-router.php?poll-url-name=$1

# Route requests for forms applying to a specific resource.
RewriteRule	^/polls/([a-z0-9\-]+)/(delete|edit)$		/polls/$2.php?poll-url-name=$1
RewriteRule	^/polls/([a-z0-9\-]+)/votes/new$			/poll-votes/new.php?poll-url-name=$1

# Route requests for a `PollVote'.
RewriteRule	^/polls/([a-z0-9\-]+)/votes/([0-9]+)$		/poll-votes/http-router.php?poll-url-name=$1&user-id=$2

# List all `PollVote`s for a given `Poll`.
RewriteCond	%{REQUEST_METHOD} ^(GET|HEAD)$
RewriteRule	^/polls/([a-z0-9\-]+)/votes$			/polls/poll-votes/index.php?poll-url-name=$1

# Route non-GET/HEAD requests for the resource root.
RewriteCond	%{REQUEST_METHOD} !^(GET|HEAD)$
RewriteRule	^/polls/([a-z0-9\-]+)/votes$			/poll-votes/http-router.php?poll-url-name=$1
